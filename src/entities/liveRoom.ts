export class LiveRoom{
  constructor(
    public title : string,
    public description : string,
    public channelId : number,
    public watchingNumber : number,
    public teacherName ? : string,
    public id ? : number
  ){
  }
}
